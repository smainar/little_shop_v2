require 'rails_helper'

RSpec.describe "User cancels an order", type: :feature do
  it "if order is pending there is a button to cancel" do

  end

  xit "if order is not pending there is no button to cancel" do

  end
end